package com.example.loggerdoc;


import org.junit.Test;

import static junit.framework.TestCase.assertTrue;
import static org.junit.Assert.assertEquals;

public class RecordPhotoUnitTest {
    /*
    @Test
    public void TestsetPhoto() {
        String image = "someimage.png";
        RecordPhoto recordPhoto = new RecordPhoto();
        recordPhoto.setPhoto(image);
        RecordPhotoList list = new RecordPhotoList();
        list.add(recordPhoto);
        assertTrue("size should be 1", list.contains(recordPhoto));

    }
    @Test
    public void TestgetPhoto() {
        String image = "someimage.png";
        RecordPhoto recordPhoto = new RecordPhoto();
        recordPhoto.setPhoto(image);
        String photo = recordPhoto.getPhoto();
        assertEquals("Should be same file name", image, photo);

    }*/
}


